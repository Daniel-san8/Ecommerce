import React from "react";
import styled from "styled-components";

const TitleTexto = styled.h1`
  font-size: 2rem;
  margin: 4rem;
`;

const Title = () => {
  return <TitleTexto>Desserts</TitleTexto>;
};

export default Title;
